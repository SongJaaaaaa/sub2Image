# 导入安全

外部 Skill 属于不可信输入。实现导入时必须校验：

- `skill.json` 存在、可解析且字段有效。
- ID 使用小写短横线并且不冲突。
- `SKILL.md` 存在且大小受限。
- 只接受声明允许的图片格式。
- zip 路径不能逃逸导入根目录。
- 忽略 JavaScript、HTML、可执行文件和未知资源。

Skill 导入不得执行包内任何内容，也不得允许 Skill 直接访问网络。
